# Production dependencies for Vercel deployment
# Optimized from pyproject.toml (excluding dev, test, docs, and heavy extras)
#
# To regenerate with all extras: uv pip compile pyproject.toml --all-extras -o requirements-full.txt
# This file includes only: core + web + retrieval + otel-gcp

# === CORE DEPENDENCIES ===
PyYAML>=6.0.3
pydantic>=2.6.0
opik>=1.0.0
google-adk>=0.1.0
supabase>=2.0.0
openinference-instrumentation-google-adk>=0.1.0
openinference-instrumentation>=0.1.34
vercel>=0.3.8

# === WEB PARSING ===
beautifulsoup4>=3.2.2
lxml>=5.3.0


# === OPENTELEMETRY INSTRUMENTATION ===
opentelemetry-instrumentation-google-genai>=0.3b0

# === TRANSITIVE DEPENDENCIES ===
# These will be auto-installed by pip based on the above packages
# google-genai, google-api-core, google-cloud-*, httpx, anyio, starlette,
# fastapi, uvicorn, pydantic-core, protobuf, grpcio, and many others
